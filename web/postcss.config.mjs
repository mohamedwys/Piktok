/**
 * PostCSS configuration for Tailwind + Autoprefixer.
 * The standard Next.js + Tailwind v3 stack.
 */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
